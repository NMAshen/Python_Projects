# Area in triangle

base = float(input("Enter your triangle base: "))
height = float(input("Enter your triangle height: "))

area = 1/2 * base * height

print(f"Your triangle area is {round(area, 2)}")