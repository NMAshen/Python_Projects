# Trapezium Area

side1 = float(input("Enter first side length :"))
side2 = float(input("Enter second side length :"))
base = float(input("Enter base length :"))

area = (side1 + side2)/2 * base

print(f"Your trapeziumArea is {round(area, 2)}")